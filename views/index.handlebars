<h1>Pizza Pizza!</h1>

<br>
<br>
<h2>Type of Pizza you would like to add</h2>
<form class="create-form">

  <div class="form-group">
    {{!-- <label for="ca">New Pizza Name:</label> --}}
    <input type="text" class="inputBox" id="ca" name="name">
  </div>
  <br>

  <button type="submit" class="addBtn">Add Pizza</button>
</form>

<h2>Pizza that's not eaten yet!</h2>

<ul>
  {{#each slices}}
    {{#unless eaten}}
      {{> slices/slice-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>
<br>
<br>
<h2>Pizza that's already eaten!</h2>

<ul>
  {{#each slices}}
    {{#if eaten}}
      {{> slices/eatenslices eaten=false}}
    {{/if}}
  {{/each}}
</ul><br>

  {{!-- <div class="form-group">
    <label for="eaten">Eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="eaten" value="0"> Not Yet!
  </div> --}}

